// variaveis e tipos de dados

const nome = 'Alexandre'     // str
const idade = 25             // number
const fazFaculdade = true    // boolean
const nulo = null            // nulo , que é definido pelo programador 
const indefinido = undefined // undefined, é definido pelo js quando você só declara uma variavel sem atribuir um valor à ela

console.log(nome, idade, fazFaculdade, nulo, indefinido)

console.log(typeof(nome)) // mostra o tipo da variavel